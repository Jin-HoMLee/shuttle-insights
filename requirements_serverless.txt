# Serverless API Requirements
# Minimal dependencies for BST model inference

# Core API framework
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Essential ML dependencies
numpy>=1.24.0

# Model inference backends (one or both)
torch>=2.0.0  # For TorchScript models
onnxruntime>=1.15.0  # For ONNX models

# Google Cloud Functions support
functions-framework>=3.0.0

# Optional: For AWS Lambda deployment
# aws-lambda-powertools[pydantic]