name: "Weekly Refactor Issue"
on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Create weekly refactor issue
        uses: peter-evans/create-issue@v5
        with:
          title: "Weekly Refactor - ${{ github.run_id }}"
          labels: refactor, recurring
          assignees: Jin-HoMLee
          body: |
            ## Weekly Refactor Checklist
            <!-- checklist here -->
